#!/bin/bash
set -eu

bail() {
	echo >&2 $*
	exit 1
}

if [[ -z ${1:-} ]]; then
	bail "Please specify a version to release\n$0 x.y.\nz";
fi
VERSION=$1
if [[ -z $PERL5LIB ]]; then
	PERL5LIB=lib
else
	PERL5LIB=lib:$PERL5LIB
fi

echo "Packaging verse v$VERSION..."
./pack $VERSION
